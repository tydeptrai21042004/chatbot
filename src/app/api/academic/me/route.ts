import{NextRequest,NextResponse}from"next/server";import{readIdentity}from"@/src/server/auth/auth";import{findUserByEmail,studentRecords}from"@/src/server/data/store";
export async function GET(req:NextRequest){const id=readIdentity(req);if(!id||id.role!=="student"||!id.email)return NextResponse.json({ok:false,error:"Forbidden"},{status:403});const u=await findUserByEmail(id.email);if(!u)return NextResponse.json({ok:false,error:"User not found"},{status:404});return NextResponse.json({ok:true,records:await studentRecords(u)});}
